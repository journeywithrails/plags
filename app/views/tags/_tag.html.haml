%tr
  %td= tag.name
  %td= tag_group(tag)
  %td
    = link_to "Edit", edit_tag_path(tag)
    = link_to "Delete", tag_path(tag), { :method => :delete, :confirm => "Are you sure want to delete \"#{tag.name}\"?"}