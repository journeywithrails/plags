%h2#title
  Set monitoring parameters for #{@category.name}

#filter
  - semantic_form_for(:filter, :url => set_monitor_category_path(@category)) do |f|
    = render :partial => "shared/filter", :locals => { :f => f, :tags  => @tags, :sizes => @sizes, :sel_tags => @sel_tags, :sel_sizes  => @sel_sizes, :filter_button => 'Monitor'}


%p
%br

= link_to "Back", :back